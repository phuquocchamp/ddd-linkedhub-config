spring:
  profiles:
    active: git
  cloud:
    config:
      server:
        git:
          uri: https://github.com/phuquocchamp/ddd-linkedhub-config.git
          default-label: dev
          clone-on-start: true
          force-pull: true
logging:
  level:
    root: DEBUG
